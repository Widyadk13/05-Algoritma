START

DECLARE var = `string`
DECLARE same character = `Palindrome`

INPUT var
    IF `check` same with character 
        DISPLAY `True`
    ELSE `check` not same with character
        DISPLAY `False`

END